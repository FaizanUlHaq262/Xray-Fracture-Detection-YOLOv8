Run the virutal environement on python 3.10.9
install all the dependencies by using the requirement.txt file
pip install -r requirements.txt     (run this in the virtual environment terminal)
To run the flask app;
python app.py           (in the terminal)
